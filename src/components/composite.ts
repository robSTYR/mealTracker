import {Composite, ui} from "tabris";

export default class AppComposite extends Composite {
    constructor(top: number, left: number, right: number, bottom: number, background: string){
        super({top, left, right, bottom, background});

    }
};
