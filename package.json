{
  "name": "tabris_2_challenge",
  "version": "0.1.0",
  "main": "dist/app.js",
  "private": true,
  "scripts": {
    "test": "npm run build && npm run lint",
    "lint": "tslint --project . 'src/**/*.ts' 'src/**/*.tsx'",
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "start": "npm-run-all --parallel watch serve",
    "serve": "tabris serve"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "moment": "^2.22.1",
    "tabris": "^2.4.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.2",
    "tslint": "^5.9.1",
    "typescript": "^2.8.3"
  }
}
